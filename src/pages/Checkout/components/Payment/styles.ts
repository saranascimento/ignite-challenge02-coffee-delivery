import styled from 'styled-components'
import { Paper } from '../../styles'

export const PaymentWrapper = styled(Paper)`
  margin-top: 0.75rem;
`
export const Chips = styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;
  flex-wrap: wrap;
  gap: 0.75rem;
`
